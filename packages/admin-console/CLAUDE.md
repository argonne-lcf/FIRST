# Admin Console

Internal-only React SPA that reads the first-gateway control plane and renders
what's happening. Served in production from behind the **same nginx** as the
FastAPI gateway, so the app and API share an origin.

## Stack

- Vite + React 19 + TypeScript (`type: module`, ESM only)
- Routing: `@tanstack/react-router` — **code-defined routes in `src/router.tsx`**.
  No file-based routing: do not scaffold a `routes/` directory or
  `routeTree.gen.ts`; add routes directly in `router.tsx`.
- Auth: Globus (`@globus/react-auth-context` + `@globus/sdk`), PKCE public client
- Fetch client + TanStack Query: generated by `@hey-api/openapi-ts`
  (fetch-based, with generated query options in
  `src/lib/client/@tanstack/react-query.gen.ts`)
- Styling: Tailwind v4 + shadcn/ui (see UI / Styling below)

## Imports

- Always use the `@/` alias (`@/queries/workflows`, `@/components/ui/button`),
  never relative `../` paths.
- From `@/lib/client`: **types only** in pages/components
  (e.g. `import { type UserAuthEvent } from "@/lib/client"`).
  Runtime imports of SDK functions or generated query options belong in
  `src/queries/` (queries) or `src/lib/api.ts` (client config). This is
  lint-enforced via `no-restricted-imports`.

## Generated / do not edit

- `src/lib/client/` — entirely generated; regenerate with `npm run gen-sdk`
  instead of editing. Excluded from ESLint.

## Commands (run inside this package dir)

- `npm run lint` — ESLint
- `npm run format` — Prettier
- `npm run check` — TypeScript → ESLint → Prettier
- `npm run dev` — Vite dev server on **port 4040** (strict), base path `/admin-console`
- `npm run build` — `tsc --noEmit` then `vite build`
- `npm run gen-sdk` — regenerate the API client from the gateway's OpenAPI

**After any code change: `npm run format && npm run check`. Both must pass
before considering a task done.** If `check` fails after an SDK regen, fix
compile errors in `src/queries/` first — that's the intended blast radius of a
schema change. Never "fix" errors by editing generated files or loosening types.

## Regenerating the SDK (`npm run gen-sdk`)

- Pulls from `http://localhost:8000/openapi.json` — **the gateway must be
  running** (`make compose-up` at the repo root brings up the stack on :8000).
- Output goes to `src/lib/client/` — see Generated / do not edit above.

## Auth wiring

`src/lib/api.ts` is the seam between Globus and the generated client.

- `AuthBinding` (`src/lib/AuthBinding.tsx`, mounted in `main.tsx`) bridges the
  live `AuthorizationManager` into the module via `bindAuthorization`.
- `api.ts` calls `client.setConfig({ baseUrl: "", auth: () => <raw access_token> })`.
  - `baseUrl: ""` → same-origin relative requests (matches the nginx deployment).
  - `auth` returns the **raw** token; the generated code adds the `Bearer ` prefix.

Config IDs and scopes live in `src/config.ts` (`AUTH_CLIENT_ID`,
`GATEWAY_CLIENT_ID`, `GATEWAY_SCOPE`, `REDIRECT_URI`).

## Using Queries

- All UI components import query options from `src/queries/` — never from
  `@/lib/client/@tanstack/react-query.gen` directly (lint-enforced).
- Add needed queries to `src/queries/`, one file per resource. If a query is a
  trivial passthrough, just re-export the generated options (with a cleaner,
  shorter name).
- The generated functions already contain the `queryKey` (derived
  deterministically) and the `queryFn` that calls the fetch SDK and throws on
  errors — layer only cache policy and shaping on top.
- Use this layer to:
  - Set per-resource cache policy (`refetchInterval`, `staleTime`) in one place,
    not per component
  - Add `select` for shaping/filtering derived data
  - Write queries the codegen can't express — composite fetches, client-side
    joins, anything not 1:1 with an endpoint
- For mutations, spread the generated `*Mutation()` options and invalidate
  using the generated `*QueryKey()` functions so keys can never drift.

```ts
import { queryOptions } from "@tanstack/react-query";
import {
  getWorkflowsOptions,
  getWorkflowOptions,
} from "@/lib/client/@tanstack/react-query.gen";

export const workflowQueries = {
  // Pass-through with policy override
  all: () =>
    queryOptions({
      ...getWorkflowsOptions(),
      refetchInterval: 10_000,
      staleTime: 5_000,
    }),

  // Parameterized — forward the typed options object
  detail: (id: string) =>
    queryOptions({
      ...getWorkflowOptions({ path: { workflow_id: id } }),
      staleTime: 30_000,
    }),
};
```

Components never know whether a given query is a thin wrap, a `select`
projection, or fully hand-written.

## UI / Styling

Stack: Tailwind v4 + shadcn/ui (Radix-based). No other styling systems.

Pages compose: `src/queries/` for data, `@/components/ui/` for presentation.

### Rules

- **Never write vanilla CSS** or add to styles.css (theme variables only).
  All styling is Tailwind utility classes in TSX.
- **Check `src/components/ui/` first.** Before building any UI element,
  see if a shadcn component exists there. Compose existing components
  before writing custom ones.
- **Missing shadcn component?** Install it rather than hand-rolling:
  `npx shadcn@latest add <name>` (e.g. tabs, tooltip, dropdown-menu).
- **Use `cn()` from `@/lib/utils`** for conditional/merged classes,
  never template-string class concatenation.
- **Use theme tokens, not raw colors**: `bg-background`, `text-muted-foreground`,
  `border-border`, `text-destructive`, etc. Never `bg-white`, `text-gray-500`,
  or arbitrary hex values — this keeps dark mode working for free.
- Components in `components/ui/` are ours and editable, but prefer
  variant props (`cva`) over one-off overrides. If you need a new visual
  variant of Button/Badge, add it to the variants definition in the
  component file so it's reusable.
- components use React 19 ref-as-prop; do not add forwardRef

### Layout conventions

- Page shells: `p-6 space-y-6` (or `gap-6` with flex/grid).
- Spacing scale: stick to 2/4/6/8 steps; avoid odd one-off values.
- Data display: use `<Card>` for panels, `<Table>` for tabular state,
  `<Badge>` for statuses, `<Skeleton>` for loading states
  (not spinners or "Loading..." text).
- Status colors: map through Badge variants, defined once in badge.tsx —
  don't scatter status→color logic across pages.

### Imports

- UI components: `import { Button } from '@/components/ui/button'`
- Never import from Radix (`@radix-ui/*`) directly in pages;
  go through `components/ui/`.

### Live Testing with Playwright MCP

- `npm run dev`: start background vite dev server on http://localhost:4040
(already proxies API calls to localhost:8000 correctly)
- Use Playwright MCP to interact with this dev deployment